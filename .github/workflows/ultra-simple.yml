name: Ultra Simple CI

on: [push, pull_request]

jobs:
  simple-build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Print hello
      run: echo "✅ Hello from Emier Blockchain - Basic CI Works!"
      
    - name: List files
      run: |
        echo "📁 Project structure:"
        find . -name "*.rs" -o -name "*.toml" | head -20
        echo "✅ File listing complete"
